# 変更履歴

このファイルは Memo Studio の主な変更を記録します。バージョン表記はアプリ右上の
「Memo Studio」ロゴの横に表示されます。

## v1.4.2 — 2026-09-07

### 不具合修正

- **保存ボタンをダブルクリックすると、同じメモが 2 件作成される不具合を修正
  しました。** 新規メモの保存は「登録して id を受け取る」処理のため、1 回目が
  終わる前に 2 回目が始まると、どちらも「まだ登録されていない」と判断して
  それぞれ別のメモを作っていました。保存処理を直列化し、2 回目は 1 回目の
  完了を待ってから更新として動くようにしました
  - `Ctrl+S` の押しっぱなし（キーリピート）でも同じ現象が起きていたため、
    あわせて修正しました
  - **フォーマットの保存**にも同じ原因の二重登録があり、こちらも修正しました
- **本文の文字数カウントに、書式や画像の内部マーカーが含まれていた不具合を
  修正しました。** 文字を太字にしただけで「5 文字」が「12 文字」に増え、
  画像を 1 枚貼ると 13 文字ぶん多く数えられていました。画面に見えている
  文字だけを数えるようにしています
- **本文に貼った画像が、別の画像を追加・削除したあとに表示できなくなることが
  ある不具合を修正しました。** 画像パネルを読み込み直すたびに、まだ本文で
  使われている画像の参照（Blob URL）まで破棄していたことが原因です。表示中の
  メモに残っている画像の参照は保持し、別のメモへ移った画像・削除された画像の
  ぶんだけを解放するようにしました
- **音声入力中にタイトル欄などへ触れると、確定した文字が本文の先頭に
  割り込む不具合を修正しました。** 本文の外にカーソルがある状態では、
  本文の末尾へ追記するようにしています
- **確認ダイアログを開くボタンをダブルクリックすると、ダイアログが一瞬で
  閉じてしまう不具合を修正しました。** 2 回目のクリックが、開いた直後の
  ダイアログの背面に当たっていたためで、削除ボタンを素早く 2 回押すと削除が
  実行されませんでした
- **画像パネルの幅をドラッグで調整したとき、下限・上限を越えて動かすと範囲外の
  値（負の幅など）が設定として保存される問題を修正しました**（表示のたびに
  クランプしていたため、見た目の不具合は出ていません）

### 保存に失敗したときの案内

- 保存領域の空き容量が足りないなどで**保存に失敗しても何も表示されず、
  保存できたのかどうか分からない**状態でした。失敗時はエラーを表示し、
  「未保存の変更があります」の表示も維持するようにしました
- 未保存の変更がある状態で「保存して続行」を選んだとき、保存に失敗した場合は
  画面を切り替えないようにしました（編集内容がそのまま失われるのを防ぎます）
- 画像の登録・削除が失敗した場合もエラーを表示します

### 内部構造の整理（リファクタリング）
機能・画面の見た目に変更はありません。今後の変更を追いやすくするための整理です。

- **`script.js`（3,300 行）を役割ごとに 11 個のファイルへ分割しました**
  （`js/core.js` `js/list.js` `js/editor.js` `js/body.js` `js/text-format.js`
  `js/ctxmenu.js` `js/images.js` `js/speech.js` `js/formats.js` `js/data.js`
  `js/main.js`）。ビルド不要・`index.html` を開くだけという方針は変えていません
  （ES モジュールは `file://` で読み込めないため、通常の `<script>` として
  依存順に読み込みます）
- 各所に散らばっていた同じ処理を共通のヘルパーへまとめました
  - 本文編集後の表示更新（19 箇所）→ `afterBodyEdit()`
  - 書式適用時のテキストノードのラップ処理（約 30 行 × 2）→ `wrapTextNodes()`
  - 選択範囲の復元・検証・再設定 → `requireBodySelection()` ほか
  - 祖先要素の探索（5 箇所）→ `closestFormatEl()`
  - 件数の集計（5 箇所）／確認ダイアログ（6 箇所）→ `countBy()` / `confirmDialog()`
- 上記に伴い、フォーマット（テンプレート）を適用したときに改行マークが
  更新されないなど、処理の抜けによる小さな不整合も解消しました
- 到達不能だった分岐や重複していた見出しコメントを整理しました

## v1.4.1 — 2026-09-07

### 不具合修正
- **複数行を選択してリンクを設定すると、改行が失われて1行につながってしまう
  不具合を修正しました。** リンクダイアログの「表示する文字」欄が1行入力の
  ため、複数行の内容を流し込んだ時点で改行が捨てられていたことが原因です
  - 文字を選択してリンクを設定する場合は「表示する文字」欄を出さず、
    選択した内容（改行や書式を含む）をそのまま活かすようにしました
  - 複数行にリンクを設定すると、行ごとにリンクが分かれます
    （インライン要素は行をまたげないため）
  - 文字を選択していない場合は、これまでどおり URL と表示文字の両方を
    指定して挿入できます

## v1.4.0 — 2026-08-25

### 書式が元に戻せない不具合の修正
- **文字サイズ・文字色・太さを変更したあと元に戻せなくなる不具合を修正しました。**
  これまでは書式の解除が「書式要素まるごと」を単位に行われていたため、
  文字の一部だけを選んで解除しようとすると効かなかったり、逆に選んでいない
  文字の書式まで外れたりしていました。選択した文字だけを正確に対象とするよう
  作り直し、次のいずれの操作でも確実に元へ戻せるようになりました
  - 文字の一部だけを選んで解除する
  - 大きな範囲に適用したあと、その内側の一部だけ解除する
  - サイズや色を何度も変更したあとに解除する
  - 太字・斜体を部分的に解除する
- あわせて、書式を変更するたびに内部のラッパー要素が入れ子で積み重なり、
  見た目は変わっても古い指定が残り続ける問題も解消しました（同じ種類の書式は
  常に1つに保たれます）
- 解除の結果として空になった書式要素が保存データに残る問題も修正しました

### 標準に戻す機能
- **選択範囲を「黒・標準サイズ・太さ普通」に一括で戻す機能を追加しました。**
  ツールバーの書式解除ボタン、および右クリックメニューの「標準に戻す」から
  実行できます
- 右クリックメニューの文字色パレットにある「既定（黒）」も、色指定そのものを
  外して本文の地の色に正確に戻します

### 右クリックメニューの再構成
よく使う操作をすぐ押せる位置に集約し、それ以外はまとめて折りたたみました。
- **上段（よく使う）**：切り取り／コピー／貼り付け
- **書式**：太字・斜体（切替）、文字サイズ5段階、文字色パレット6色、標準に戻す
- **リンク・画像**：リンクの挿入・編集、画像の挿入
- **折りたたみ**：「挿入」（日付・時刻・箇条書き等）と「文字の変換」
  （全角半角変換・囲み・文字数）は既定で折りたたみ、必要なときだけ開けます
  （開閉状態は保持されます）

### リンク機能の追加
- 本文に**リンクを挿入・編集・解除**できるようになりました。URL と表示文字を
  指定でき、スキームを省略すると `https://` を、メールアドレスなら `mailto:` を
  補います
- リンクは **Ctrl+クリック（Mac は ⌘+クリック）** で新しいタブで開きます
- `javascript:` のような実行を伴う URL は受け付けません
- リンクはメモと一緒に保存され、エクスポート／インポートでも引き継がれます

### 目印（付箋）の表示改善
- 画像アイコンと同じように、**日付グループの見出しにも目印を集約表示**する
  ようにしました。折りたたんだままでも、その日にどの目印が付いているか
  （同じ目印が複数あれば件数も）が分かります

## v1.3.0 — 2026-08-25

### 今回追加された機能・修正（このバージョンで新たに反映）
- **日付グループを折りたたんでも、編集中のメモと目印（付箋）付きのメモは
  一覧に残るようにしました**。折りたたむと今どれを開いているのか分からなく
  なる問題を解消し、あわせて目印機能と連動させて「付箋を付けたメモは
  折り目からはみ出したまま残る」動作にしています
  - 残ったメモは少し内側に寄せ、破線で折りたたみ中のグループにつながって
    いることを示します
  - 隠れているメモは「他 N 件を表示」から展開できます（グループ見出しと
    同じくクリックで開きます）
  - 編集中のメモを含むグループは、見出しのラベルの色と丸い印でも示すため、
    一覧をスクロールしていても選択位置を追えます
  - 検索・タグ・目印で絞り込み中は、従来どおり折りたたみを無視して
    該当メモをすべて表示します

## v1.2.0 — 2026-08-25

### 今回追加された機能・修正（このバージョンで新たに反映）
- **右クリックメニューに書式設定を追加**：本文を選択して右クリックすると、
  「太字（切替）」「斜体（切替）」と、文字サイズ（小・標準・大・特大・最大・解除）を
  その場で適用できるようになりました。ツールバーまで移動せずに書式を変更できます。
  選択範囲が無いときは自動的に無効表示になります
- **メモに目印（付箋）を付けられる機能を追加**：メモごとに「重要・要対応・完了・
  保留・ピン・注意」の6種類から目印を1つ付けられます。同じ目印をもう一度押すか
  「×」で外せます。付けた目印は一覧のメモに色付きのアイコンと左端の色帯で表示され、
  サイドバーの目印バーから「その目印が付いたメモだけ」を絞り込めます。
  目印はメモと一緒に保存され、エクスポート／インポートでも引き継がれます

### 不具合修正
- 複数行を選択して太字・斜体を解除しようとしても解除できない不具合を修正
  （書式適用時に残る空のテキストノードが「すべて書式済み」の判定を
  妨げていたため）
- 書式を解除した直後などに、カーソル行のハイライトが本文エリア全体を
  覆ってしまう表示崩れを修正
- 項目が増えた右クリックメニューが、画面の低いウィンドウで見切れないよう
  画面内に収めてスクロールできるようにしました

## v1.1.0 — 2026-07-14

### 今回追加された機能・修正（このバージョンで新たに反映）
- **書式設定を可逆化**：
  - 文字色・ハイライト・文字サイズを再適用すると、内部的に古いラッパー要素が
    残り続け、完全に元へ戻せなくなる不具合を修正。同じ種類の書式がすでに
    かかっている範囲は、新たに入れ子にするのではなくその場でスタイルを
    書き換えるように変更
  - 上記が原因で、文字サイズを標準へ戻しても行間（line-height）が戻らない
    不具合も解消
  - 太字・斜体は選択範囲の状態に応じて一括ON/OFFする本来のトグル動作に変更
  - 文字色・ハイライトに解除ボタン、文字サイズに「サイズ解除」を追加し、
    値ベースの書式も明示的に「書式なし」へ戻せるように対応
- **ツールバーを再設計**：2段に分かれていた書式ツールバーを1段に統合し、
  縦方向の情報密度を向上（既定幅で高さ約104px→50pxに削減）。画面が狭い
  ときは自動的に折り返し、操作できないボタンが出ないよう対応

## v1.0.0 — 2026-07-12

初めてバージョン番号を導入したリリースです。それまでの開発（Initial commit 以降、
無番号のまま積み重ねてきた全ての機能・修正）を一つの安定版としてまとめ、
今後はここからバージョンを刻んでいきます。

### 今回追加された機能・修正（このバージョンで新たに反映）
- **書式設定機能を追加**：本文編集エリアに新しいツールバーを追加し、太字・斜体・
  文字サイズ（小/標準/大/特大/最大）・文字色・ハイライト色を選択範囲に適用できる
  ようになりました。書式は入れ子にでき（例：太字＋色付き文字）、既存の画像機能と
  同じ「プレーンテキスト＋マーカー」形式で保存されるため、検索やスニペット表示にも
  正しく反映されます
- **上記機能の堅牢性検証と修正**：
  - 閉じタグの無い書式表記（本文に文字通り `[b]` とだけ入力した場合など）で
    保存・再読み込み時にアプリがフリーズする重大な不具合を修正
  - 本文を全選択して書式を適用した際に、画像コントロールの内部要素まで
    書式化されてしまう不具合を修正
  - 選択を解除した後にツールバーの書式ボタンを押すと、直前の選択範囲に
    書式が誤って再適用される不具合を修正
- **アプリ名の横にバージョン番号を表示**：今回追加

### これまでに積み重ねてきた主な機能（v1.0.0 に含まれる）
- **メモ管理**：新規作成・保存・削除、タイトル／タグ／本文編集、日付グループ化
  （作成日・更新日切替、折りたたみ）、更新順ソート（昇順・降順切替）
- **検索**：タイトル・タグ・本文を対象にした検索、検索範囲の絞り込み、
  Google 風の検索サジェスト、日付グループ折りたたみ時も検索結果が隠れない対応
- **タグ・フォーマット管理**：タグマスタとカスタムフォーマットを統合した管理画面、
  フォーマットの動的トークン、本文への適用
- **画像機能**：本文内へのインライン画像配置（ドラッグ移動・コーナードラッグでの
  リサイズ・整列・サイズプリセット「幅に合わせる」等）、画像パネルでの一覧管理
  （サムネイルサイズ調整・パネル幅のドラッグ変更・画像のみ絞り込み表示）、
  ダブルクリックでの拡大ビューワ、クリップボードへの画像コピー
- **本文エディタの視認性向上**：改行マークの表示切替、カーソル行のハイライト表示
- **入出力**：JSON データのインポート／エクスポート、ドラッグ＆ドロップ取り込み、
  他アプリ形式（Minutes Memo Pro）のインポート対応
- **音声入力**：Web Speech API を使った本文への直接音声入力
- **UI**：サイドバー／画像パネルの開閉、ファビコン

### 既知の制約
- 書式のクリア（書式解除ボタン）は、選択範囲の一部だけがクリックされた場合でも
  その書式要素全体を解除する簡易な実装です（文字単位の精密な分割は行いません）
